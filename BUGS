- b-level priority computation (for STGs) assume same communication cost
  for each interconnect. The result is worse if the assumption is not held.
  The algorithm computes the result based on the first interconnect.
  This does not affect KPNs.
